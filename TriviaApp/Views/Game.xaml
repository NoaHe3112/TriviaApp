<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TriviaApp.Views.Game">
    <ContentPage.Content>
        <StackLayout>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="2*"/>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Label x:Name="Question" Text="{Binding Question}" FontSize="Header" HorizontalOptions="CenterAndExpand"
                       Grid.Row="0" Grid.ColumnSpan="2" Grid.Column="0" />
                <Button x:Name="Option1" Text="{Binding Options[0]}" Command="{Binding OptionClicked}" Grid.Column="0"
                        Grid.Row="1" BackgroundColor="{Binding Color1}"/>
                <Button x:Name="Option2" Text="{Binding Options[1]}" Command="{Binding OptionClicked}" Grid.Column="1"
                        Grid.Row="1" BackgroundColor="{Binding Color2}"/>
                <Button x:Name="Option3" Text="{Binding Options[2]}" Command="{Binding OptionClicked}" Grid.Column="0"
                        Grid.Row="2" BackgroundColor="{Binding Color3}"/>
                <Button x:Name="Option4" Text="{Binding Options[3]}" Command="{Binding OptionClicked}" Grid.Column="1"
                        Grid.Row="2" BackgroundColor="{Binding Color4}"/>

            </Grid>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>